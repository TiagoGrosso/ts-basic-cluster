{
  "name": "simple-cluster",
  "version": "0.1.0",
  "description": "Run multiple async tasks in parallel",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "build": "tsc"
  }
}
